<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nhnent.repository.NoteMapper">

	<select id="selectList" resultType="Note">
		SELECT
		 id AS id,
		 useremail AS userEmail,
		 password AS password
		FROM note
	</select>
	
	<select id="selectOne" resultType="Note" parameterType="Long">
		SELECT
		 id AS id,
		 useremail AS userEmail,
		 password AS password
		FROM note
		WHERE note.id = #{value}
	</select>
	
	<select id="selectByUserEmail" resultType="Note">
		SELECT
		 id AS id,
		 useremail AS userEmail,
		 password AS password
		FROM note
		WHERE note.userEmail = #{userEmail}
	</select>
	
	<insert id="insert" parameterType="com.nhnent.repository.entity.Note"  useGeneratedKeys="true">
		INSERT INTO note (
		    id,
			useremail,
			password,
			bodytext,
			registeredDate
		) 
		VALUES (
			#{id},
			#{userEmail},
			#{password},
			#{bodyText},
			#{registeredDate}
		)
	</insert>
	
	<update id="update" parameterType="com.nhnent.repository.entity.Note">
		UPDATE note SET
		useremail = #{userEmail},
		password = #{password},
		bodytext = #{bodyText},
		modifiedDate = #{modifiedDate}
		WHERE note.id = #{id}
	</update>
	
	<delete id="delete" parameterType="com.nhnent.repository.entity.Note">
		DELETE FROM note WHERE note.id = #{id}
	</delete>
	
</mapper> 